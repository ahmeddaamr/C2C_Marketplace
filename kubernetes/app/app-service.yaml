apiVersion: v1
kind: Service
metadata:
  labels:
    tier: app
  namespace: c2c-marketplace
  name: app-service
spec:
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    tier: app
  type: ClusterIP
status:
  loadBalancer: {}
